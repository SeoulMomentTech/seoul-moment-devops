CloudFormation 템플릿을 분석하여 아키텍처 다이어그램(HTML)을 생성합니다.

다음 명령을 실행하세요:

```bash
python "$PROJECT_DIR/.claude/skills/cf-diagram/scripts/generate_diagram.py" --dir "$PROJECT_DIR" --output "$PROJECT_DIR/cf-architecture-diagram.html"
```

생성된 HTML 파일을 브라우저에서 열어주세요.
